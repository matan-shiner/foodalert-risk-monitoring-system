# Weekly Coverage Audit — 2026-09-12 — RUN FAILED (network egress blocked)

## What happened

This week's scheduled audit could not be performed. Both secondary sources
required by this audit are blocked by this session's network egress policy:

- `https://efoodalert.com/feed/` — CONNECT tunnel rejected, HTTP 403 (policy denial)
- `https://www.foodsafetynews.com/feed/` — CONNECT tunnel rejected, HTTP 403 (policy denial)

The egress proxy status endpoint confirmed both as `connect_rejected` /
"gateway answered 403 to CONNECT (policy denial or upstream failure)" for
`efoodalert.com:443` and `www.foodsafetynews.com:443`. Per this environment's
own proxy guidance, a 403 from the gateway means the destination host is not
allowed by the organization's egress policy for this session, and the
correct response is to report the blocked host rather than retry or route
around it. No workaround was attempted (no alternate mirrors, caches, or
third-party fetchers were used) since that would violate that guidance and
risk fetching an unverified/unofficial rendition of the source content.

Note: last week's run (2026-09-09) *did* successfully reach both feeds, so
this is a change in this session's network policy/allowlist, not a
persistent limitation of the two sources themselves.

## Impact on tracker state

- **`audits/gap_tracker.json` was intentionally left unmodified.** `last_run`
  remains `2026-09-09` and `pending_gaps` is unchanged. Advancing `last_run`
  to today without actually checking any content would silently create a
  coverage hole (any secondary-source recalls published between 2026-09-09
  and whenever egress is restored would never be evaluated). Leaving
  `last_run` at 2026-09-09 means the next successful run will naturally
  cover the full gap period.
- The three items already pending from the 2026-09-09 run (Marché d'ici et
  d'ailleurs / MAPAQ chili sauce; FreshPoint Cranberry Chicken Salad Wedge
  sandwiches; Lemond/Seoul Trading Ofood Ssamjang paste) were **not**
  re-checked this week and remain at `checks_missing: 1` each, unchanged.

## Action needed

This audit cannot run again until `efoodalert.com` and
`www.foodsafetynews.com` are allowed through this session's/environment's
egress policy. Please have an administrator add these two hosts to the
allowlist, then re-run (or wait for) the next scheduled audit.
